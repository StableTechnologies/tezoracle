# Asset and policy configuration

This directory holds the version-controlled parameter register.

No production, shadow, or testnet asset policy is committed in the repository
skeleton. Draft, testnet, shadow, and non-authoritative stub configs are added
with the payload and evidence freeze.
