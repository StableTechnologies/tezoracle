/**
 * Class A TypeScript validator placeholder.
 *
 * Signing is blocked until packing golden vectors pass (payload freeze + parity).
 * This module exists so TypeScript CI has an importable surface today.
 */
export const TEZORACLE_VERSION = "0.0.0";
export const STATUS = "non-production" as const;
